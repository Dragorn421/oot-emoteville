/*
 * DEF_ROOM(name, segmentName)
 * - `name` is the room's name in the room id enum, to be used for example in
 *   table_spawns.h
 * - `segmentName` is the name of the room segment in the spec
 */

DEF_ROOM(MY_MAP_SCENE_ROOM_ROOM_0, my_map_scene_room_0)
