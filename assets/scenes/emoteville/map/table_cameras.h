#include "camera.h"

/*
 * DEF_CAMERA(name, cameraSetting)
 * - `name` is the camera's name in the camera indices enum, to be used for
 *   example in table_polytypes.h
 */

DEF_CAMERA(MY_MAP_SCENE_CAMERA_DEFAULT, CAM_SET_NORMAL0)
